<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools">
  <permission android:name="com.provider_with_access_to.SIDDESH"></permission>


  <application
    android:allowBackup="true"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:fullBackupContent="@xml/backup_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.CustomContentProvider"
    tools:targetApi="31">
    <activity
      android:exported="true"
      android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

      <meta-data
        android:name="android.app.lib_name"
        android:value="" />
    </activity>
    
    <provider
      android:authorities="com.example.customcontentprovider"
      android:name="com.example.customcontentprovider.AcronymProvider"
      android:exported="true"
      android:enabled="true"
      android:grantUriPermissions="true"
      >


    </provider>
    
  </application>

</manifest>